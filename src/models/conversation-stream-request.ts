/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { ClosedEnum } from "../types/enums.js";
import {
  AgentCapabilities,
  AgentCapabilities$Outbound,
  AgentCapabilities$outboundSchema,
} from "./agent-capabilities.js";
import {
  AppliedFilters,
  AppliedFilters$Outbound,
  AppliedFilters$outboundSchema,
} from "./applied-filters.js";
import {
  ChatAttachmentRef,
  ChatAttachmentRef$Outbound,
  ChatAttachmentRef$outboundSchema,
} from "./chat-attachment-ref.js";
import {
  Filters,
  Filters$Outbound,
  Filters$outboundSchema,
} from "./filters.js";

/**
 * Optional execution mode for non-stream consumers of this shared
 *
 * @remarks
 * request schema.
 * `agent` uses the universal agent loop, while `internal_search`
 * and `web_search` use their corresponding assistant search paths.
 */
export const ConversationStreamRequestChatMode = {
  Agent: "agent",
  InternalSearch: "internal_search",
  WebSearch: "web_search",
} as const;
/**
 * Optional execution mode for non-stream consumers of this shared
 *
 * @remarks
 * request schema.
 * `agent` uses the universal agent loop, while `internal_search`
 * and `web_search` use their corresponding assistant search paths.
 */
export type ConversationStreamRequestChatMode = ClosedEnum<
  typeof ConversationStreamRequestChatMode
>;

/**
 * AG-UI is the only supported wire protocol. When present must be
 *
 * @remarks
 * `"agui"`. Omitting the field is equivalent — the server always
 * uses the AG-UI vocabulary (`RUN_STARTED`, `TEXT_MESSAGE_CONTENT`,
 * etc.). Kept in the schema for backward compatibility with callers
 * that already send it.
 */
export const ConversationStreamRequestProtocol = {
  Agui: "agui",
} as const;
/**
 * AG-UI is the only supported wire protocol. When present must be
 *
 * @remarks
 * `"agui"`. Omitting the field is equivalent — the server always
 * uses the AG-UI vocabulary (`RUN_STARTED`, `TEXT_MESSAGE_CONTENT`,
 * etc.). Kept in the schema for backward compatibility with callers
 * that already send it.
 */
export type ConversationStreamRequestProtocol = ClosedEnum<
  typeof ConversationStreamRequestProtocol
>;

/**
 * Request body for `POST /conversations/stream`. The public stream
 *
 * @remarks
 * requires an explicit universal execution mode.
 */
export type ConversationStreamRequest = {
  /**
   * The user's question or prompt to start the conversation.
   *
   * @remarks
   * Supports natural language queries of any complexity.
   */
  query: string;
  /**
   * Limit the AI's knowledge scope to specific records/documents.
   *
   * @remarks
   * When provided, only these records will be searched for context.
   */
  recordIds?: Array<string> | undefined;
  /**
   * App connector instance ids and knowledge-base / record-group ids that narrow retrieval
   *
   * @remarks
   * for a turn. For **org assistant** chat streams, send explicit `apps` / `kb` lists.
   * For **agent** chat streams, send explicit id lists, or **omit** `filters` (and `tools`)
   * to let the service use the agent’s stored knowledge and tool configuration. Sending
   * `{ "apps": [], "kb": [] }` on an agent stream means **no** knowledge sources for that
   * turn (it is not “full org default”).
   */
  filters?: Filters | undefined;
  /**
   * Rich filter state selected by the user, used for display and persistence only.
   *
   * @remarks
   * This mirrors the active selection shown in the UI and is distinct from the
   * machine-readable `filters` field used for retrieval scoping.
   */
  appliedFilters?: AppliedFilters | undefined;
  /**
   * Uploaded chat attachments to associate with this conversation turn (see
   *
   * @remarks
   * `POST /conversations/attachments/upload`).
   */
  attachments?: Array<ChatAttachmentRef> | undefined;
  /**
   * Identifier for the AI model configuration to use.
   *
   * @remarks
   * Available models depend on organization settings.
   */
  modelKey?: string | undefined;
  /**
   * Display name of the AI model
   */
  modelName?: string | undefined;
  /**
   * Friendly display name of the selected model
   */
  modelFriendlyName?: string | undefined;
  /**
   * Optional execution mode for non-stream consumers of this shared
   *
   * @remarks
   * request schema.
   * `agent` uses the universal agent loop, while `internal_search`
   * and `web_search` use their corresponding assistant search paths.
   */
  chatMode: ConversationStreamRequestChatMode;
  /**
   * IANA timezone identifier from the client (top-level field).
   *
   * @remarks
   * Used to provide time-aware context to the AI.
   */
  timezone?: string | undefined;
  /**
   * ISO 8601 / RFC 3339 datetime from the client (top-level field; UTC `Z` or numeric offset).
   *
   * @remarks
   */
  currentTime?: Date | undefined;
  /**
   * Optional list of tool identifiers (fully-qualified action names such as
   *
   * @remarks
   * "jira.create_issue") that the AI agent is permitted to invoke for this
   * request. When omitted the agent may use any configured tool. Applicable
   * only when `chatMode` is `agent`.
   */
  tools?: Array<string> | undefined;
  /**
   * AG-UI is the only supported wire protocol. When present must be
   *
   * @remarks
   * `"agui"`. Omitting the field is equivalent — the server always
   * uses the AG-UI vocabulary (`RUN_STARTED`, `TEXT_MESSAGE_CONTENT`,
   * etc.). Kept in the schema for backward compatibility with callers
   * that already send it.
   */
  protocol?: ConversationStreamRequestProtocol | undefined;
  /**
   * Per-request agent capability toggles. Only meaningful when `chatMode`
   *
   * @remarks
   * selects an agent mode; ignored otherwise. Each field falls back to its
   * own `default` below when omitted — a missing flag is not uniformly
   * `true`. Omitting the whole object applies every default.
   */
  agentCapabilities?: AgentCapabilities | undefined;
};

/** @internal */
export const ConversationStreamRequestChatMode$outboundSchema: z.ZodMiniEnum<
  typeof ConversationStreamRequestChatMode
> = z.enum(ConversationStreamRequestChatMode);

/** @internal */
export const ConversationStreamRequestProtocol$outboundSchema: z.ZodMiniEnum<
  typeof ConversationStreamRequestProtocol
> = z.enum(ConversationStreamRequestProtocol);

/** @internal */
export type ConversationStreamRequest$Outbound = {
  query: string;
  recordIds?: Array<string> | undefined;
  filters?: Filters$Outbound | undefined;
  appliedFilters?: AppliedFilters$Outbound | undefined;
  attachments?: Array<ChatAttachmentRef$Outbound> | undefined;
  modelKey?: string | undefined;
  modelName?: string | undefined;
  modelFriendlyName?: string | undefined;
  chatMode: string;
  timezone?: string | undefined;
  currentTime?: string | undefined;
  tools?: Array<string> | undefined;
  protocol?: string | undefined;
  agentCapabilities?: AgentCapabilities$Outbound | undefined;
};

/** @internal */
export const ConversationStreamRequest$outboundSchema: z.ZodMiniType<
  ConversationStreamRequest$Outbound,
  ConversationStreamRequest
> = z.object({
  query: z.string(),
  recordIds: z.optional(z.array(z.string())),
  filters: z.optional(Filters$outboundSchema),
  appliedFilters: z.optional(AppliedFilters$outboundSchema),
  attachments: z.optional(z.array(ChatAttachmentRef$outboundSchema)),
  modelKey: z.optional(z.string()),
  modelName: z.optional(z.string()),
  modelFriendlyName: z.optional(z.string()),
  chatMode: ConversationStreamRequestChatMode$outboundSchema,
  timezone: z.optional(z.string()),
  currentTime: z.optional(z.pipe(z.date(), z.transform(v => v.toISOString()))),
  tools: z.optional(z.array(z.string())),
  protocol: z.optional(ConversationStreamRequestProtocol$outboundSchema),
  agentCapabilities: z.optional(AgentCapabilities$outboundSchema),
});

export function conversationStreamRequestToJSON(
  conversationStreamRequest: ConversationStreamRequest,
): string {
  return JSON.stringify(
    ConversationStreamRequest$outboundSchema.parse(conversationStreamRequest),
  );
}
