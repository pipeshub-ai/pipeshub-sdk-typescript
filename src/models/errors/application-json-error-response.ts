/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import * as models from "../index.js";
import { PipeshubError } from "./pipeshub-error.js";

/**
 * Standard JSON error envelope from `ErrorMiddleware` for `BaseError` subclasses (`error.middleware.ts`).
 *
 * @remarks
 * Returned for most API 4xx errors (unauthorized, forbidden, not found, validation failures, etc.).
 */
export type ApplicationJsonErrorResponseData = {
  error: models.ApplicationJsonErrorResponseError;
};

/**
 * Standard JSON error envelope from `ErrorMiddleware` for `BaseError` subclasses (`error.middleware.ts`).
 *
 * @remarks
 * Returned for most API 4xx errors (unauthorized, forbidden, not found, validation failures, etc.).
 */
export class ApplicationJsonErrorResponse extends PipeshubError {
  error: models.ApplicationJsonErrorResponseError;

  /** The original data that was passed to this error instance. */
  data$: ApplicationJsonErrorResponseData;

  constructor(
    err: ApplicationJsonErrorResponseData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.error?.message
      || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.error = err.error;

    this.name = "ApplicationJsonErrorResponse";
  }
}

/** @internal */
export const ApplicationJsonErrorResponse$inboundSchema: z.ZodMiniType<
  ApplicationJsonErrorResponse,
  unknown
> = z.pipe(
  z.object({
    error: z.lazy(() => models.ApplicationJsonErrorResponseError$inboundSchema),
    request$: z.custom<Request>(x => x instanceof Request),
    response$: z.custom<Response>(x => x instanceof Response),
    body$: z.string(),
  }),
  z.transform((v) => {
    return new ApplicationJsonErrorResponse(v, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  }),
);
