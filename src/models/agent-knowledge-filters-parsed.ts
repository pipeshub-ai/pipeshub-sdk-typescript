/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Indexed scope for a knowledge connector: record-group ids (collections /
 *
 * @remarks
 * KB roots) and individual record ids. First-party create/update flows set
 * `recordGroups` and `records`. On GET, `filtersParsed` is this shape
 * parsed from the stored `filters` JSON string.
 */
export type AgentKnowledgeFiltersParsed = {
  /**
   * Record-group ids (e.g. knowledge-base roots) in scope.
   */
  recordGroups?: Array<string> | undefined;
  /**
   * Individual record ids in scope.
   */
  records?: Array<string> | undefined;
};

/** @internal */
export const AgentKnowledgeFiltersParsed$inboundSchema: z.ZodMiniType<
  AgentKnowledgeFiltersParsed,
  unknown
> = z.object({
  recordGroups: types.optional(z.array(types.string())),
  records: types.optional(z.array(types.string())),
});
/** @internal */
export type AgentKnowledgeFiltersParsed$Outbound = {
  recordGroups?: Array<string> | undefined;
  records?: Array<string> | undefined;
};

/** @internal */
export const AgentKnowledgeFiltersParsed$outboundSchema: z.ZodMiniType<
  AgentKnowledgeFiltersParsed$Outbound,
  AgentKnowledgeFiltersParsed
> = z.object({
  recordGroups: z.optional(z.array(z.string())),
  records: z.optional(z.array(z.string())),
});

export function agentKnowledgeFiltersParsedToJSON(
  agentKnowledgeFiltersParsed: AgentKnowledgeFiltersParsed,
): string {
  return JSON.stringify(
    AgentKnowledgeFiltersParsed$outboundSchema.parse(
      agentKnowledgeFiltersParsed,
    ),
  );
}
export function agentKnowledgeFiltersParsedFromJSON(
  jsonString: string,
): SafeParseResult<AgentKnowledgeFiltersParsed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentKnowledgeFiltersParsed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentKnowledgeFiltersParsed' from JSON`,
  );
}
