/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import * as types from "../../types/primitives.js";
import { PipeshubError } from "./pipeshub-error.js";

/**
 * Error payload returned by the legacy record-stream proxy when the downstream
 *
 * @remarks
 * streaming request fails after route middleware has passed.
 */
export type StreamRecordErrorResponseData = {
  /**
   * Human-readable error message from the gateway or downstream stream service
   */
  error: string;
};

/**
 * Error payload returned by the legacy record-stream proxy when the downstream
 *
 * @remarks
 * streaming request fails after route middleware has passed.
 */
export class StreamRecordErrorResponse extends PipeshubError {
  /**
   * Human-readable error message from the gateway or downstream stream service
   */
  error: string;

  /** The original data that was passed to this error instance. */
  data$: StreamRecordErrorResponseData;

  constructor(
    err: StreamRecordErrorResponseData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = "message" in err && typeof err.message === "string"
      ? err.message
      : `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.error = err.error;

    this.name = "StreamRecordErrorResponse";
  }
}

/** @internal */
export const StreamRecordErrorResponse$inboundSchema: z.ZodMiniType<
  StreamRecordErrorResponse,
  unknown
> = z.pipe(
  z.object({
    error: types.string(),
    request$: z.custom<Request>(x => x instanceof Request),
    response$: z.custom<Response>(x => x instanceof Response),
    body$: z.string(),
  }),
  z.transform((v) => {
    return new StreamRecordErrorResponse(v, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  }),
);
