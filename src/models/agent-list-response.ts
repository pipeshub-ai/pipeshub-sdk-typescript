/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  AgentListItem,
  AgentListItem$inboundSchema,
} from "./agent-list-item.js";
import {
  AgentListPagination,
  AgentListPagination$inboundSchema,
} from "./agent-list-pagination.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Paginated response returned by `GET /agents`.
 *
 * @remarks
 *
 * The Node gateway forwards the Python backend response on success. If
 * the backend returns a non-200 response, the gateway still returns HTTP
 * 200 with `success: true`, an empty `agents` array, and a zeroed
 * pagination block derived from the requested `page` / `limit`.
 */
export type AgentListResponse = {
  success: boolean;
  agents: Array<AgentListItem>;
  /**
   * Pagination block returned by `GET /agents`.
   */
  pagination: AgentListPagination;
};

/** @internal */
export const AgentListResponse$inboundSchema: z.ZodMiniType<
  AgentListResponse,
  unknown
> = z.object({
  success: types.boolean(),
  agents: z.array(AgentListItem$inboundSchema),
  pagination: AgentListPagination$inboundSchema,
});

export function agentListResponseFromJSON(
  jsonString: string,
): SafeParseResult<AgentListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentListResponse' from JSON`,
  );
}
