/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * All authentication steps complete; JWT tokens returned
 */
export type AuthenticateFinalResponse = {
  /**
   * Success message
   */
  message: string;
  /**
   * JWT access token (1 hour expiry)
   */
  accessToken: string;
  /**
   * JWT refresh token (7 days expiry)
   */
  refreshToken: string;
};

/** @internal */
export const AuthenticateFinalResponse$inboundSchema: z.ZodMiniType<
  AuthenticateFinalResponse,
  unknown
> = z.object({
  message: types.string(),
  accessToken: types.string(),
  refreshToken: types.string(),
});

export function authenticateFinalResponseFromJSON(
  jsonString: string,
): SafeParseResult<AuthenticateFinalResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AuthenticateFinalResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AuthenticateFinalResponse' from JSON`,
  );
}
