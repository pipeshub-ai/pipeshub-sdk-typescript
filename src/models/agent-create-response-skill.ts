/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A skill linked to the agent at creation time. Creating or updating an
 *
 * @remarks
 * agent only links edges to skills that already exist and never writes a
 * skill document, so only the name is echoed back here.
 */
export type AgentCreateResponseSkill = {
  name: string;
};

/** @internal */
export const AgentCreateResponseSkill$inboundSchema: z.ZodMiniType<
  AgentCreateResponseSkill,
  unknown
> = z.object({
  name: types.string(),
});

export function agentCreateResponseSkillFromJSON(
  jsonString: string,
): SafeParseResult<AgentCreateResponseSkill, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentCreateResponseSkill$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentCreateResponseSkill' from JSON`,
  );
}
