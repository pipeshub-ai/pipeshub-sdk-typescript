/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A personal access token as seen by an org admin — extends
 *
 * @remarks
 * `PatListItem` with who it belongs to, since an admin is looking
 * across every member's tokens rather than just their own.
 */
export type AdminPatListItem = {
  /**
   * Token ID
   */
  id: string;
  /**
   * Token name
   */
  name: string;
  /**
   * Granted scopes
   */
  scopes: Array<string>;
  createdAt: Date;
  /**
   * Expiry timestamp. A `"never"`-expiry token is stored as a
   *
   * @remarks
   * ~100-year-out date, not a literal null — treat anything decades
   * out as "never" rather than a real deadline.
   */
  expiresAt: Date;
  /**
   * Last time this token successfully authenticated a request.
   *
   * @remarks
   * Throttled server-side to update at most once per 5 minutes per
   * token; absent if the token has never been used.
   */
  lastUsedAt?: Date | undefined;
  /**
   * ID of the user who created this token
   */
  userId: string;
  /**
   * Owner's email. Populated even when `ownerDeleted` is true
   *
   * @remarks
   * (last-known value), for auditing.
   */
  ownerEmail?: string | undefined;
  /**
   * Owner's full name (same last-known-value behavior as `ownerEmail`)
   */
  ownerFullName?: string | undefined;
  /**
   * True if the owning user has been removed from the org (or no
   *
   * @remarks
   * longer resolves at all). The token still appears — a deleted
   * user's tokens stop authenticating automatically, but stay
   * visible here so an admin can audit/clean them up.
   */
  ownerDeleted: boolean;
};

/** @internal */
export const AdminPatListItem$inboundSchema: z.ZodMiniType<
  AdminPatListItem,
  unknown
> = z.object({
  id: types.string(),
  name: types.string(),
  scopes: z.array(types.string()),
  createdAt: types.date(),
  expiresAt: types.date(),
  lastUsedAt: types.optional(types.date()),
  userId: types.string(),
  ownerEmail: types.optional(types.string()),
  ownerFullName: types.optional(types.string()),
  ownerDeleted: types.boolean(),
});

export function adminPatListItemFromJSON(
  jsonString: string,
): SafeParseResult<AdminPatListItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AdminPatListItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AdminPatListItem' from JSON`,
  );
}
