/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  SemanticSearchHistoryFiltersApplied,
  SemanticSearchHistoryFiltersApplied$inboundSchema,
} from "./semantic-search-history-filters-applied.js";
import {
  SemanticSearchHistoryFiltersAvailable,
  SemanticSearchHistoryFiltersAvailable$inboundSchema,
} from "./semantic-search-history-filters-available.js";

export type SemanticSearchHistoryFilters = {
  /**
   * Echo of which filters the caller actually supplied, built by
   *
   * @remarks
   * `buildFiltersMetadata` (utils.ts:430-486). `page` and `limit` always
   * appear because they are normalised to defaults before being recorded,
   * so `filters` is never empty and `values` always contains at least
   * `{ page, limit }`. Other keys appear only when the matching query
   * param was non-empty (or, for `dateRange`, when `createdAt` was set
   * on the Mongo filter).
   *
   * `values` keys are scalar strings rather than typed primitives
   * (`'true'`/`'false'`, `'5'`, etc.) because they are passed through
   * from `req.query` as Express parsed them — only `page` and `limit`
   * are coerced to integers via `safeParsePagination`.
   */
  applied: SemanticSearchHistoryFiltersApplied;
  /**
   * Catalogue of filters the endpoint supports, plus their current values
   *
   * @remarks
   * and `applied` flags. Built by `buildFiltersMetadata` (utils.ts:430-624).
   */
  available: SemanticSearchHistoryFiltersAvailable;
};

/** @internal */
export const SemanticSearchHistoryFilters$inboundSchema: z.ZodMiniType<
  SemanticSearchHistoryFilters,
  unknown
> = z.object({
  applied: SemanticSearchHistoryFiltersApplied$inboundSchema,
  available: SemanticSearchHistoryFiltersAvailable$inboundSchema,
});

export function semanticSearchHistoryFiltersFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchHistoryFilters, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SemanticSearchHistoryFilters$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchHistoryFilters' from JSON`,
  );
}
