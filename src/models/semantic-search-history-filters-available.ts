/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  SemanticSearchHistoryDateRange,
  SemanticSearchHistoryDateRange$inboundSchema,
} from "./semantic-search-history-date-range.js";
import {
  SemanticSearchHistoryFilterToggle,
  SemanticSearchHistoryFilterToggle$inboundSchema,
} from "./semantic-search-history-filter-toggle.js";
import {
  SemanticSearchHistoryPaginationField,
  SemanticSearchHistoryPaginationField$inboundSchema,
} from "./semantic-search-history-pagination-field.js";
import {
  SemanticSearchHistorySortField,
  SemanticSearchHistorySortField$inboundSchema,
} from "./semantic-search-history-sort-field.js";

export type SemanticSearchHistoryFiltersAvailablePagination = {
  page: SemanticSearchHistoryPaginationField;
  limit: SemanticSearchHistoryPaginationField;
};

export type Sorting = {
  /**
   * Used for `available.sorting.{sortBy,sortOrder}` and
   *
   * @remarks
   * `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
   * is present on `sorting.*` and absent on `sortingMessages.*`, so it
   * is optional here.
   */
  sortBy: SemanticSearchHistorySortField;
  /**
   * Used for `available.sorting.{sortBy,sortOrder}` and
   *
   * @remarks
   * `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
   * is present on `sorting.*` and absent on `sortingMessages.*`, so it
   * is optional here.
   */
  sortOrder: SemanticSearchHistorySortField;
};

export type DateFilters = {
  dateRange: SemanticSearchHistoryDateRange;
};

export type MessageFilters = {
  /**
   * Generic "filter X is available, current value is Y" block used for
   *
   * @remarks
   * `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
   * `type` (free-form value) or `values` (enum of allowed strings) is
   * present, not both. `current` is the caller-supplied value passed
   * through from `req.query`, hence string-or-null even when `type` is
   * `'number'`.
   */
  messageType: SemanticSearchHistoryFilterToggle;
};

export type SortingMessages = {
  /**
   * Used for `available.sorting.{sortBy,sortOrder}` and
   *
   * @remarks
   * `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
   * is present on `sorting.*` and absent on `sortingMessages.*`, so it
   * is optional here.
   */
  sortBy: SemanticSearchHistorySortField;
  /**
   * Used for `available.sorting.{sortBy,sortOrder}` and
   *
   * @remarks
   * `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
   * is present on `sorting.*` and absent on `sortingMessages.*`, so it
   * is optional here.
   */
  sortOrder: SemanticSearchHistorySortField;
};

/**
 * Catalogue of filters the endpoint supports, plus their current values
 *
 * @remarks
 * and `applied` flags. Built by `buildFiltersMetadata` (utils.ts:430-624).
 */
export type SemanticSearchHistoryFiltersAvailable = {
  /**
   * Generic "filter X is available, current value is Y" block used for
   *
   * @remarks
   * `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
   * `type` (free-form value) or `values` (enum of allowed strings) is
   * present, not both. `current` is the caller-supplied value passed
   * through from `req.query`, hence string-or-null even when `type` is
   * `'number'`.
   */
  shared: SemanticSearchHistoryFilterToggle;
  /**
   * Generic "filter X is available, current value is Y" block used for
   *
   * @remarks
   * `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
   * `type` (free-form value) or `values` (enum of allowed strings) is
   * present, not both. `current` is the caller-supplied value passed
   * through from `req.query`, hence string-or-null even when `type` is
   * `'number'`.
   */
  tags: SemanticSearchHistoryFilterToggle;
  /**
   * Generic "filter X is available, current value is Y" block used for
   *
   * @remarks
   * `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
   * `type` (free-form value) or `values` (enum of allowed strings) is
   * present, not both. `current` is the caller-supplied value passed
   * through from `req.query`, hence string-or-null even when `type` is
   * `'number'`.
   */
  minMessages: SemanticSearchHistoryFilterToggle;
  /**
   * Generic "filter X is available, current value is Y" block used for
   *
   * @remarks
   * `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
   * `type` (free-form value) or `values` (enum of allowed strings) is
   * present, not both. `current` is the caller-supplied value passed
   * through from `req.query`, hence string-or-null even when `type` is
   * `'number'`.
   */
  search: SemanticSearchHistoryFilterToggle;
  pagination: SemanticSearchHistoryFiltersAvailablePagination;
  sorting: Sorting;
  dateFilters: DateFilters;
  messageFilters: MessageFilters;
  sortingMessages: SortingMessages;
};

/** @internal */
export const SemanticSearchHistoryFiltersAvailablePagination$inboundSchema:
  z.ZodMiniType<SemanticSearchHistoryFiltersAvailablePagination, unknown> = z
    .object({
      page: SemanticSearchHistoryPaginationField$inboundSchema,
      limit: SemanticSearchHistoryPaginationField$inboundSchema,
    });

export function semanticSearchHistoryFiltersAvailablePaginationFromJSON(
  jsonString: string,
): SafeParseResult<
  SemanticSearchHistoryFiltersAvailablePagination,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      SemanticSearchHistoryFiltersAvailablePagination$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'SemanticSearchHistoryFiltersAvailablePagination' from JSON`,
  );
}

/** @internal */
export const Sorting$inboundSchema: z.ZodMiniType<Sorting, unknown> = z.object({
  sortBy: SemanticSearchHistorySortField$inboundSchema,
  sortOrder: SemanticSearchHistorySortField$inboundSchema,
});

export function sortingFromJSON(
  jsonString: string,
): SafeParseResult<Sorting, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Sorting$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Sorting' from JSON`,
  );
}

/** @internal */
export const DateFilters$inboundSchema: z.ZodMiniType<DateFilters, unknown> = z
  .object({
    dateRange: SemanticSearchHistoryDateRange$inboundSchema,
  });

export function dateFiltersFromJSON(
  jsonString: string,
): SafeParseResult<DateFilters, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DateFilters$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DateFilters' from JSON`,
  );
}

/** @internal */
export const MessageFilters$inboundSchema: z.ZodMiniType<
  MessageFilters,
  unknown
> = z.object({
  messageType: SemanticSearchHistoryFilterToggle$inboundSchema,
});

export function messageFiltersFromJSON(
  jsonString: string,
): SafeParseResult<MessageFilters, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MessageFilters$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MessageFilters' from JSON`,
  );
}

/** @internal */
export const SortingMessages$inboundSchema: z.ZodMiniType<
  SortingMessages,
  unknown
> = z.object({
  sortBy: SemanticSearchHistorySortField$inboundSchema,
  sortOrder: SemanticSearchHistorySortField$inboundSchema,
});

export function sortingMessagesFromJSON(
  jsonString: string,
): SafeParseResult<SortingMessages, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SortingMessages$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SortingMessages' from JSON`,
  );
}

/** @internal */
export const SemanticSearchHistoryFiltersAvailable$inboundSchema: z.ZodMiniType<
  SemanticSearchHistoryFiltersAvailable,
  unknown
> = z.object({
  shared: SemanticSearchHistoryFilterToggle$inboundSchema,
  tags: SemanticSearchHistoryFilterToggle$inboundSchema,
  minMessages: SemanticSearchHistoryFilterToggle$inboundSchema,
  search: SemanticSearchHistoryFilterToggle$inboundSchema,
  pagination: z.lazy(() =>
    SemanticSearchHistoryFiltersAvailablePagination$inboundSchema
  ),
  sorting: z.lazy(() => Sorting$inboundSchema),
  dateFilters: z.lazy(() => DateFilters$inboundSchema),
  messageFilters: z.lazy(() => MessageFilters$inboundSchema),
  sortingMessages: z.lazy(() => SortingMessages$inboundSchema),
});

export function semanticSearchHistoryFiltersAvailableFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchHistoryFiltersAvailable, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      SemanticSearchHistoryFiltersAvailable$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchHistoryFiltersAvailable' from JSON`,
  );
}
