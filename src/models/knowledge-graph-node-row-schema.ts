/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * One row in a navigate listing. Carries the identity fields of
 *
 * @remarks
 * KnowledgeGraphNodeRefSchema (inlined rather than composed, so this
 * object can stay closed) plus display detail.
 */
export type KnowledgeGraphNodeRowSchema = {
  id: string;
  name: string;
  /**
   * One of `recordGroup`, `folder`, `record`. Listings never contain `app` nodes.
   */
  nodeType: string;
  subType: string | null;
  isRecord: boolean;
  /**
   * Whether this node can itself be opened with `nodeId`.
   */
  hasChildren: boolean;
  /**
   * e.g. size, indexing status, or relationship type.
   */
  detail: string | null;
  /**
   * Link to the record in its source system. Records that carry a relative URL (uploaded/KB records) are resolved against the configured frontend public URL (see POST /configurationManager/frontendPublicUrl). The value is null when that host is not configured.
   */
  webUrl?: string | null | undefined;
  indexingStatus?: string | null | undefined;
  /**
   * Creation time in the source system, epoch milliseconds. Null when the underlying node did not carry one.
   */
  sourceCreatedAt?: number | null | undefined;
  sourceModifiedAt?: number | null | undefined;
  /**
   * 1-based nesting depth from the navigated parent. Always 1 for a `depth=1` listing; 2 or 3 for deeper rows when `depth` > 1.
   */
  level?: number | undefined;
  contextSummary?: string | null | undefined;
};

/** @internal */
export const KnowledgeGraphNodeRowSchema$inboundSchema: z.ZodMiniType<
  KnowledgeGraphNodeRowSchema,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    node_type: types.string(),
    sub_type: types.nullable(types.string()),
    is_record: types.boolean(),
    has_children: types.boolean(),
    detail: types.nullable(types.string()),
    web_url: z.optional(z.nullable(types.string())),
    indexing_status: z.optional(z.nullable(types.string())),
    source_created_at: z.optional(z.nullable(types.number())),
    source_modified_at: z.optional(z.nullable(types.number())),
    level: types.optional(types.number()),
    context_summary: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "node_type": "nodeType",
      "sub_type": "subType",
      "is_record": "isRecord",
      "has_children": "hasChildren",
      "web_url": "webUrl",
      "indexing_status": "indexingStatus",
      "source_created_at": "sourceCreatedAt",
      "source_modified_at": "sourceModifiedAt",
      "context_summary": "contextSummary",
    });
  }),
);

export function knowledgeGraphNodeRowSchemaFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeGraphNodeRowSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeGraphNodeRowSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeGraphNodeRowSchema' from JSON`,
  );
}
