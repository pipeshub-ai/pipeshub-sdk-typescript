/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  PersistedSemanticSearchSharedWithEntry,
  PersistedSemanticSearchSharedWithEntry$inboundSchema,
} from "./persisted-semantic-search-shared-with-entry.js";

/**
 * One persisted search row as returned by `GET /search`. Mirrors the
 *
 * @remarks
 * persisted search document except `citationIds` is an array of
 * ObjectId strings (citations are not populated on the list endpoint,
 * unlike `GET /search/{searchId}`).
 *
 * `shareLink` is absent from the JSON when the search has not been
 * shared. `archivedBy` is `null` on rows that were previously archived
 * and then unarchived, and absent on rows that have never been
 * archived. The list endpoint already filters out currently-archived
 * rows, so a string user-id value never appears here in practice.
 */
export type SemanticSearchHistoryItem = {
  id: string;
  v: number;
  query: string;
  limit: number;
  orgId: string;
  userId: string;
  citationIds: Array<string>;
  /**
   * Map of source-record id (or `_key`) to `JSON.stringify(<record>)`.
   *
   * @remarks
   * Clients must `JSON.parse` each value to recover the underlying
   * record object. Populated at write-time in the POST `/search` handler.
   */
  records: { [k: string]: string };
  isShared: boolean;
  /**
   * Set once the search has been shared via `/search/{searchId}/share`.
   */
  shareLink?: string | undefined;
  sharedWith: Array<PersistedSemanticSearchSharedWithEntry>;
  isArchived: boolean;
  /**
   * User ID of the last user who archived this row, or `null` after
   *
   * @remarks
   * an unarchive cleared the archive state. Absent on rows that have
   * never been archived.
   */
  archivedBy?: string | null | undefined;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const SemanticSearchHistoryItem$inboundSchema: z.ZodMiniType<
  SemanticSearchHistoryItem,
  unknown
> = z.pipe(
  z.object({
    _id: types.string(),
    __v: types.number(),
    query: types.string(),
    limit: types.number(),
    orgId: types.string(),
    userId: types.string(),
    citationIds: z.array(types.string()),
    records: z.record(z.string(), types.string()),
    isShared: types.boolean(),
    shareLink: types.optional(types.string()),
    sharedWith: z.array(PersistedSemanticSearchSharedWithEntry$inboundSchema),
    isArchived: types.boolean(),
    archivedBy: z.optional(z.nullable(types.string())),
    createdAt: types.date(),
    updatedAt: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_id": "id",
      "__v": "v",
    });
  }),
);

export function semanticSearchHistoryItemFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchHistoryItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SemanticSearchHistoryItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchHistoryItem' from JSON`,
  );
}
