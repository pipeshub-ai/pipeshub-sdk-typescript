/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  SemanticSearchHistoryAppliedDateRange,
  SemanticSearchHistoryAppliedDateRange$inboundSchema,
} from "./semantic-search-history-applied-date-range.js";

export const Filter = {
  Page: "page",
  Limit: "limit",
  Search: "search",
  Shared: "shared",
  Tags: "tags",
  MinMessages: "minMessages",
  SortBy: "sortBy",
  SortOrder: "sortOrder",
  StartDate: "startDate",
  EndDate: "endDate",
  MessageType: "messageType",
  DateRange: "dateRange",
} as const;
export type Filter = OpenEnum<typeof Filter>;

export type Values = {
  page?: number | undefined;
  limit?: number | undefined;
  search?: string | undefined;
  shared?: string | undefined;
  tags?: string | undefined;
  minMessages?: string | undefined;
  sortBy?: string | undefined;
  sortOrder?: string | undefined;
  startDate?: string | undefined;
  endDate?: string | undefined;
  messageType?: string | undefined;
  /**
   * Echoed back only when the caller passed `startDate` and/or `endDate`.
   *
   * @remarks
   * Each bound is an ISO 8601 string when set; the field is absent when
   * the corresponding query param was omitted (utils.ts:480-486 reads
   * `appliedFilters.createdAt.$gte?.toISOString()` directly, so missing
   * bounds become `undefined` and drop out of the JSON).
   */
  dateRange?: SemanticSearchHistoryAppliedDateRange | undefined;
};

/**
 * Echo of which filters the caller actually supplied, built by
 *
 * @remarks
 * `buildFiltersMetadata` (utils.ts:430-486). `page` and `limit` always
 * appear because they are normalised to defaults before being recorded,
 * so `filters` is never empty and `values` always contains at least
 * `{ page, limit }`. Other keys appear only when the matching query
 * param was non-empty (or, for `dateRange`, when `createdAt` was set
 * on the Mongo filter).
 *
 * `values` keys are scalar strings rather than typed primitives
 * (`'true'`/`'false'`, `'5'`, etc.) because they are passed through
 * from `req.query` as Express parsed them — only `page` and `limit`
 * are coerced to integers via `safeParsePagination`.
 */
export type SemanticSearchHistoryFiltersApplied = {
  filters: Array<Filter>;
  values: Values;
};

/** @internal */
export const Filter$inboundSchema: z.ZodMiniType<Filter, unknown> = openEnums
  .inboundSchema(Filter);

/** @internal */
export const Values$inboundSchema: z.ZodMiniType<Values, unknown> = z.object({
  page: types.optional(types.number()),
  limit: types.optional(types.number()),
  search: types.optional(types.string()),
  shared: types.optional(types.string()),
  tags: types.optional(types.string()),
  minMessages: types.optional(types.string()),
  sortBy: types.optional(types.string()),
  sortOrder: types.optional(types.string()),
  startDate: types.optional(types.string()),
  endDate: types.optional(types.string()),
  messageType: types.optional(types.string()),
  dateRange: types.optional(
    SemanticSearchHistoryAppliedDateRange$inboundSchema,
  ),
});

export function valuesFromJSON(
  jsonString: string,
): SafeParseResult<Values, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Values$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Values' from JSON`,
  );
}

/** @internal */
export const SemanticSearchHistoryFiltersApplied$inboundSchema: z.ZodMiniType<
  SemanticSearchHistoryFiltersApplied,
  unknown
> = z.object({
  filters: z.array(Filter$inboundSchema),
  values: z.lazy(() => Values$inboundSchema),
});

export function semanticSearchHistoryFiltersAppliedFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchHistoryFiltersApplied, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      SemanticSearchHistoryFiltersApplied$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchHistoryFiltersApplied' from JSON`,
  );
}
