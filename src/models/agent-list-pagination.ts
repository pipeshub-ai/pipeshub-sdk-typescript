/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Pagination block returned by `GET /agents`.
 */
export type AgentListPagination = {
  /**
   * Current 1-based page number.
   */
  currentPage: number;
  /**
   * Page size actually applied by the backend.
   */
  limit: number;
  /**
   * Total number of matching agents across all pages.
   */
  totalItems: number;
  /**
   * Total number of pages for the current query.
   */
  totalPages: number;
  /**
   * Whether a later page exists.
   */
  hasNext: boolean;
  /**
   * Whether an earlier page exists.
   */
  hasPrev: boolean;
};

/** @internal */
export const AgentListPagination$inboundSchema: z.ZodMiniType<
  AgentListPagination,
  unknown
> = z.object({
  currentPage: types.number(),
  limit: types.number(),
  totalItems: types.number(),
  totalPages: types.number(),
  hasNext: types.boolean(),
  hasPrev: types.boolean(),
});

export function agentListPaginationFromJSON(
  jsonString: string,
): SafeParseResult<AgentListPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentListPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentListPagination' from JSON`,
  );
}
