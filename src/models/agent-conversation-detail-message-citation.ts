/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { Citation, Citation$inboundSchema } from "./citation.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Citation entry returned inside a conversation message after the
 *
 * @remarks
 * handler populates `messages.citations.citationId` and rewrites each
 * item to `{ citationId, citationData }`.
 */
export type AgentConversationDetailMessageCitation = {
  citationId?: string | undefined;
  /**
   * A populated citation document. Represents a single chunk of source
   *
   * @remarks
   * content (e.g. a passage from a document or record) referenced by an
   * AI response, together with its provenance metadata.
   */
  citationData?: Citation | undefined;
};

/** @internal */
export const AgentConversationDetailMessageCitation$inboundSchema:
  z.ZodMiniType<AgentConversationDetailMessageCitation, unknown> = z.object({
    citationId: types.optional(types.string()),
    citationData: types.optional(Citation$inboundSchema),
  });

export function agentConversationDetailMessageCitationFromJSON(
  jsonString: string,
): SafeParseResult<AgentConversationDetailMessageCitation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      AgentConversationDetailMessageCitation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentConversationDetailMessageCitation' from JSON`,
  );
}
