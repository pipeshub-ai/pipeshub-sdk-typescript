/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Minimal identity reference for a node in the knowledge graph.
 */
export type KnowledgeGraphNodeRefSchema = {
  /**
   * Node identifier. Pass it back as `nodeId` to open this node. When `is_record` is true it is also the Record ID accepted by GET /connectors/record/{recordId}/content.
   */
  id: string;
  name: string;
  /**
   * One of `app`, `recordGroup`, `folder`, `record`.
   */
  nodeType: string;
  /**
   * e.g. `TICKET`, `CONFLUENCE_PAGE`, `PROJECT`, `COLLECTION`.
   */
  subType: string | null;
  /**
   * True for record and folder nodes — the nodes whose content can be read via GET /connectors/record/{recordId}/content.
   */
  isRecord: boolean;
};

/** @internal */
export const KnowledgeGraphNodeRefSchema$inboundSchema: z.ZodMiniType<
  KnowledgeGraphNodeRefSchema,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    node_type: types.string(),
    sub_type: types.nullable(types.string()),
    is_record: types.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "node_type": "nodeType",
      "sub_type": "subType",
      "is_record": "isRecord",
    });
  }),
);

export function knowledgeGraphNodeRefSchemaFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeGraphNodeRefSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeGraphNodeRefSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeGraphNodeRefSchema' from JSON`,
  );
}
