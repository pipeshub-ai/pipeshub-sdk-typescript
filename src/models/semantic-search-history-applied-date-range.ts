/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Echoed back only when the caller passed `startDate` and/or `endDate`.
 *
 * @remarks
 * Each bound is an ISO 8601 string when set; the field is absent when
 * the corresponding query param was omitted (utils.ts:480-486 reads
 * `appliedFilters.createdAt.$gte?.toISOString()` directly, so missing
 * bounds become `undefined` and drop out of the JSON).
 */
export type SemanticSearchHistoryAppliedDateRange = {
  start?: Date | undefined;
  end?: Date | undefined;
};

/** @internal */
export const SemanticSearchHistoryAppliedDateRange$inboundSchema: z.ZodMiniType<
  SemanticSearchHistoryAppliedDateRange,
  unknown
> = z.object({
  start: types.optional(types.date()),
  end: types.optional(types.date()),
});

export function semanticSearchHistoryAppliedDateRangeFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchHistoryAppliedDateRange, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      SemanticSearchHistoryAppliedDateRange$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchHistoryAppliedDateRange' from JSON`,
  );
}
