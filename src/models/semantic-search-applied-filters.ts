/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Present when KB filters were applied to the search request.
 */
export type SemanticSearchAppliedFilters = {
  kb: Array<string>;
  kbCount: number;
};

/** @internal */
export const SemanticSearchAppliedFilters$inboundSchema: z.ZodMiniType<
  SemanticSearchAppliedFilters,
  unknown
> = z.pipe(
  z.object({
    kb: z.array(types.string()),
    kb_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "kb_count": "kbCount",
    });
  }),
);

export function semanticSearchAppliedFiltersFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchAppliedFilters, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SemanticSearchAppliedFilters$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchAppliedFilters' from JSON`,
  );
}
