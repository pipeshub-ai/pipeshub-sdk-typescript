/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Used for `available.sorting.{sortBy,sortOrder}` and
 *
 * @remarks
 * `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
 * is present on `sorting.*` and absent on `sortingMessages.*`, so it
 * is optional here.
 */
export type SemanticSearchHistorySortField = {
  values: Array<string>;
  default: string;
  description: string;
  current: string;
  applied?: boolean | undefined;
};

/** @internal */
export const SemanticSearchHistorySortField$inboundSchema: z.ZodMiniType<
  SemanticSearchHistorySortField,
  unknown
> = z.object({
  values: z.array(types.string()),
  default: types.string(),
  description: types.string(),
  current: types.string(),
  applied: types.optional(types.boolean()),
});

export function semanticSearchHistorySortFieldFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchHistorySortField, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SemanticSearchHistorySortField$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchHistorySortField' from JSON`,
  );
}
