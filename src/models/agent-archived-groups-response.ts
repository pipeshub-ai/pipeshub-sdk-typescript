/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  AgentArchivedConversationGroup,
  AgentArchivedConversationGroup$inboundSchema,
} from "./agent-archived-conversation-group.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import { RequestMeta, RequestMeta$inboundSchema } from "./request-meta.js";
import {
  SemanticSearchHistoryPagination,
  SemanticSearchHistoryPagination$inboundSchema,
} from "./semantic-search-history-pagination.js";

/**
 * Response from `GET /agents/conversations/show/archives` — archived agent
 *
 * @remarks
 * conversations grouped by `agentKey`, with agent-level pagination over
 * the groups and a fixed slice of conversations under each agent.
 */
export type AgentArchivedGroupsResponse = {
  groups: Array<AgentArchivedConversationGroup>;
  /**
   * Pagination block emitted by `buildPaginationMetadata` (utils.ts:417).
   *
   * @remarks
   * `totalPages` is `Math.ceil(totalCount / limit)`, so an empty result
   * has `totalPages: 0`, not `1`.
   */
  agentPagination: SemanticSearchHistoryPagination;
  /**
   * Basic request metadata returned by the API.
   */
  meta: RequestMeta;
};

/** @internal */
export const AgentArchivedGroupsResponse$inboundSchema: z.ZodMiniType<
  AgentArchivedGroupsResponse,
  unknown
> = z.object({
  groups: z.array(AgentArchivedConversationGroup$inboundSchema),
  agentPagination: SemanticSearchHistoryPagination$inboundSchema,
  meta: RequestMeta$inboundSchema,
});

export function agentArchivedGroupsResponseFromJSON(
  jsonString: string,
): SafeParseResult<AgentArchivedGroupsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentArchivedGroupsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentArchivedGroupsResponse' from JSON`,
  );
}
