/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type McpServerTool = {
  /**
   * Tool node key in the backing graph store.
   */
  key?: string | undefined;
  name?: string | undefined;
  fullName?: string | undefined;
  description?: string | undefined;
};

/**
 * MCP server instance linked to an agent, as projected by the graph
 *
 * @remarks
 * store on `GET /agents/{agentKey}` and `GET /agents` — same shape as
 * `Toolset`. MCP server nodes carry no secrets, only the attach-time
 * snapshot of `instanceId`/`typeId`/`name`.
 */
export type McpServer = {
  /**
   * MCP server instance node key in the backing graph store.
   */
  key?: string | undefined;
  /**
   * MCP server attachment name (attach-time snapshot).
   */
  name?: string | undefined;
  /**
   * Human-readable MCP server product label (for example `Jira MCP`).
   */
  displayName?: string | undefined;
  /**
   * Catalog server type id, when this instance came from a registered template.
   */
  typeId?: string | undefined;
  /**
   * Admin-created MCP server instance id.
   */
  instanceId?: string | undefined;
  tools?: Array<McpServerTool> | undefined;
};

/** @internal */
export const McpServerTool$inboundSchema: z.ZodMiniType<
  McpServerTool,
  unknown
> = z.pipe(
  z.object({
    _key: types.optional(types.string()),
    name: types.optional(types.string()),
    fullName: types.optional(types.string()),
    description: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_key": "key",
    });
  }),
);

export function mcpServerToolFromJSON(
  jsonString: string,
): SafeParseResult<McpServerTool, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpServerTool$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpServerTool' from JSON`,
  );
}

/** @internal */
export const McpServer$inboundSchema: z.ZodMiniType<McpServer, unknown> = z
  .pipe(
    z.object({
      _key: types.optional(types.string()),
      name: types.optional(types.string()),
      displayName: types.optional(types.string()),
      typeId: types.optional(types.string()),
      instanceId: types.optional(types.string()),
      tools: types.optional(z.array(z.lazy(() => McpServerTool$inboundSchema))),
    }),
    z.transform((v) => {
      return remap$(v, {
        "_key": "key",
      });
    }),
  );

export function mcpServerFromJSON(
  jsonString: string,
): SafeParseResult<McpServer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => McpServer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'McpServer' from JSON`,
  );
}
