/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export const ConversationMessageStreamSSEEventEvent = {
  RunStarted: "RUN_STARTED",
  RunFinished: "RUN_FINISHED",
  RunError: "RUN_ERROR",
  StepStarted: "STEP_STARTED",
  StepFinished: "STEP_FINISHED",
  TextMessageStart: "TEXT_MESSAGE_START",
  TextMessageContent: "TEXT_MESSAGE_CONTENT",
  TextMessageEnd: "TEXT_MESSAGE_END",
  ReasoningStart: "REASONING_START",
  ReasoningMessageStart: "REASONING_MESSAGE_START",
  ReasoningMessageContent: "REASONING_MESSAGE_CONTENT",
  ReasoningMessageEnd: "REASONING_MESSAGE_END",
  ReasoningEnd: "REASONING_END",
  ToolCallStart: "TOOL_CALL_START",
  ToolCallArgs: "TOOL_CALL_ARGS",
  ToolCallEnd: "TOOL_CALL_END",
  ToolCallResult: "TOOL_CALL_RESULT",
  StateDelta: "STATE_DELTA",
  StateSnapshot: "STATE_SNAPSHOT",
  Custom: "CUSTOM",
  Heartbeat: "HEARTBEAT",
} as const;
export type ConversationMessageStreamSSEEventEvent = OpenEnum<
  typeof ConversationMessageStreamSSEEventEvent
>;

/**
 * Server-Sent Event envelope for public follow-up streams using `agent`,
 *
 * @remarks
 * `internal_search`, or `web_search` on an existing conversation. AG-UI
 * is the sole wire protocol.
 *
 * `event` carries the AG-UI type name and `data` is a JSON-encoded
 * object that includes a `"type"` field matching `event`, plus
 * type-specific fields. Universal `agent` mode may emit step and nested
 * child-run events. Stable gateway-generated top-level outcomes:
 *
 * - `CUSTOM` (`name: "conversation_created"`) — fired once on
 *   connection.
 * - `RUN_FINISHED` — fired once after the AI backend finishes.
 *   The gateway emits `{ type, result }`; `result` carries
 *   `{ conversation, recordsUsed, meta }`.
 *   `recordsUsed` is the count of citations attached to the new
 *   assistant message.
 * - `RUN_ERROR` — fired when the stream fails. Carries a `message`
 *   and optional `code`; the conversation row is marked FAILED
 *   before close.
 *
 * Forwarded upstream lifecycle or child-run events may contain `runId`,
 * `threadId`, and `parentRunId`. Gateway-generated root terminal events
 * do not.
 * Clients should ignore unknown event names rather than treating them
 * as errors.
 */
export type ConversationMessageStreamSSEEvent = {
  event?: ConversationMessageStreamSSEEventEvent | undefined;
  /**
   * JSON-encoded event payload. The decoded JSON includes a `"type"`
   *
   * @remarks
   * field matching `event`, plus type-specific fields. Shape depends
   * on `event`. Forwarded lifecycle events may carry `runId`,
   * `threadId`, and `parentRunId`.
   */
  data?: string | undefined;
};

/** @internal */
export const ConversationMessageStreamSSEEventEvent$inboundSchema:
  z.ZodMiniType<ConversationMessageStreamSSEEventEvent, unknown> = openEnums
    .inboundSchema(ConversationMessageStreamSSEEventEvent);

/** @internal */
export const ConversationMessageStreamSSEEvent$inboundSchema: z.ZodMiniType<
  ConversationMessageStreamSSEEvent,
  unknown
> = z.object({
  event: types.optional(ConversationMessageStreamSSEEventEvent$inboundSchema),
  data: types.optional(types.string()),
});

export function conversationMessageStreamSSEEventFromJSON(
  jsonString: string,
): SafeParseResult<ConversationMessageStreamSSEEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConversationMessageStreamSSEEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConversationMessageStreamSSEEvent' from JSON`,
  );
}
