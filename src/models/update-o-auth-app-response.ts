/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  OAuthAppResponse,
  OAuthAppResponse$inboundSchema,
} from "./o-auth-app-response.js";

/**
 * Response body for `PUT /oauth-clients/{appId}` (`oauth.app.controller.ts` `updateApp`).
 *
 * @remarks
 * Updated app (never includes `clientSecret`) is nested under `app`.
 */
export type UpdateOAuthAppResponse = {
  message: string;
  /**
   * OAuth app details (without secret). Fields under `required:` always appear
   *
   * @remarks
   * in `toAppResponse` (`oauth.app.service.ts`); optional URL/description fields
   * are only present when set by the caller.
   */
  app: OAuthAppResponse;
};

/** @internal */
export const UpdateOAuthAppResponse$inboundSchema: z.ZodMiniType<
  UpdateOAuthAppResponse,
  unknown
> = z.object({
  message: types.string(),
  app: OAuthAppResponse$inboundSchema,
});

export function updateOAuthAppResponseFromJSON(
  jsonString: string,
): SafeParseResult<UpdateOAuthAppResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateOAuthAppResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateOAuthAppResponse' from JSON`,
  );
}
