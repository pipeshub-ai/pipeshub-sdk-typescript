/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  PersistedSemanticSearchCitationMetadata,
  PersistedSemanticSearchCitationMetadata$inboundSchema,
} from "./persisted-semantic-search-citation-metadata.js";

/**
 * Populated citation document referenced from a persisted search.
 *
 * @remarks
 * The controller strips `__v` via `populate({ select: '-__v' })`.
 */
export type PersistedSemanticSearchCitation = {
  id: string;
  content: string;
  chunkIndex: number;
  citationType: string;
  /**
   * Citation metadata as persisted in MongoDB. Required fields mirror the
   *
   * @remarks
   * Mongoose schema's `required: true` flags; the rest are optional and
   * may come through as `null` because the AI retrieval service emits
   * explicit nulls for absent fields.
   */
  metadata: PersistedSemanticSearchCitationMetadata;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const PersistedSemanticSearchCitation$inboundSchema: z.ZodMiniType<
  PersistedSemanticSearchCitation,
  unknown
> = z.pipe(
  z.object({
    _id: types.string(),
    content: types.string(),
    chunkIndex: types.number(),
    citationType: types.string(),
    metadata: PersistedSemanticSearchCitationMetadata$inboundSchema,
    createdAt: types.date(),
    updatedAt: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_id": "id",
    });
  }),
);

export function persistedSemanticSearchCitationFromJSON(
  jsonString: string,
): SafeParseResult<PersistedSemanticSearchCitation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PersistedSemanticSearchCitation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PersistedSemanticSearchCitation' from JSON`,
  );
}
