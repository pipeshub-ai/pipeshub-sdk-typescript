/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A single record resolved from an identifier.
 */
export type KnowledgeGraphLookupMatchSchema = {
  /**
   * The Record ID. Pass it to GET /connectors/record/{recordId}/content to read the record, or as `nodeId` to GET /connectors/navigate to list what is under it.
   */
  id: string;
  name: string;
  recordType: string | null;
  connectorName: string | null;
  webUrl: string | null;
  indexingStatus: string | null;
  /**
   * Which of the supplied identifiers produced this match.
   */
  identifierUsed: string;
  /**
   * e.g. `PA-1787`, a Confluence page id, a Drive file id.
   */
  externalId?: string | null | undefined;
  /**
   * Type-specific metadata for the record — for a ticket, status, assignee, priority and dates.
   */
  contextBlock?: string | null | undefined;
};

/** @internal */
export const KnowledgeGraphLookupMatchSchema$inboundSchema: z.ZodMiniType<
  KnowledgeGraphLookupMatchSchema,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.string(),
    record_type: types.nullable(types.string()),
    connector_name: types.nullable(types.string()),
    web_url: types.nullable(types.string()),
    indexing_status: types.nullable(types.string()),
    identifier_used: types.string(),
    external_id: z.optional(z.nullable(types.string())),
    context_block: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "record_type": "recordType",
      "connector_name": "connectorName",
      "web_url": "webUrl",
      "indexing_status": "indexingStatus",
      "identifier_used": "identifierUsed",
      "external_id": "externalId",
      "context_block": "contextBlock",
    });
  }),
);

export function knowledgeGraphLookupMatchSchemaFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeGraphLookupMatchSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeGraphLookupMatchSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeGraphLookupMatchSchema' from JSON`,
  );
}
