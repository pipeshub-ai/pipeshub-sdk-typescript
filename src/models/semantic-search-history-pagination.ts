/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Pagination block emitted by `buildPaginationMetadata` (utils.ts:417).
 *
 * @remarks
 * `totalPages` is `Math.ceil(totalCount / limit)`, so an empty result
 * has `totalPages: 0`, not `1`.
 */
export type SemanticSearchHistoryPagination = {
  page: number;
  limit: number;
  totalCount: number;
  totalPages: number;
  hasNextPage: boolean;
  hasPrevPage: boolean;
};

/** @internal */
export const SemanticSearchHistoryPagination$inboundSchema: z.ZodMiniType<
  SemanticSearchHistoryPagination,
  unknown
> = z.object({
  page: types.number(),
  limit: types.number(),
  totalCount: types.number(),
  totalPages: types.number(),
  hasNextPage: types.boolean(),
  hasPrevPage: types.boolean(),
});

export function semanticSearchHistoryPaginationFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchHistoryPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SemanticSearchHistoryPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchHistoryPagination' from JSON`,
  );
}
