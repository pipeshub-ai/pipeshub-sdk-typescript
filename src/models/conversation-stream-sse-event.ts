/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export const ConversationStreamSSEEventEvent = {
  RunStarted: "RUN_STARTED",
  RunFinished: "RUN_FINISHED",
  RunError: "RUN_ERROR",
  StepStarted: "STEP_STARTED",
  StepFinished: "STEP_FINISHED",
  TextMessageStart: "TEXT_MESSAGE_START",
  TextMessageContent: "TEXT_MESSAGE_CONTENT",
  TextMessageEnd: "TEXT_MESSAGE_END",
  ReasoningStart: "REASONING_START",
  ReasoningMessageStart: "REASONING_MESSAGE_START",
  ReasoningMessageContent: "REASONING_MESSAGE_CONTENT",
  ReasoningMessageEnd: "REASONING_MESSAGE_END",
  ReasoningEnd: "REASONING_END",
  ToolCallStart: "TOOL_CALL_START",
  ToolCallArgs: "TOOL_CALL_ARGS",
  ToolCallEnd: "TOOL_CALL_END",
  ToolCallResult: "TOOL_CALL_RESULT",
  StateDelta: "STATE_DELTA",
  StateSnapshot: "STATE_SNAPSHOT",
  Custom: "CUSTOM",
  Heartbeat: "HEARTBEAT",
} as const;
export type ConversationStreamSSEEventEvent = OpenEnum<
  typeof ConversationStreamSSEEventEvent
>;

/**
 * Server-Sent Event envelope for public conversation streams using
 *
 * @remarks
 * `agent`, `internal_search`, or `web_search`. AG-UI is the sole wire
 * protocol.
 *
 * `event` carries the AG-UI type name and `data` is a JSON-encoded
 * object that includes a `"type"` field matching `event`, plus
 * type-specific fields. Universal `agent` mode may emit `STEP_STARTED`,
 * `STEP_FINISHED`, and nested child-run events carrying `parentRunId`.
 * Stable gateway-generated top-level outcomes:
 *
 * - `CUSTOM` (`name: "conversation_created"`) — fired once on
 *   connection. Carries the newly created `conversationId` and
 *   `title` so the client can link the stream to the new row before
 *   any tokens arrive.
 * - `RUN_FINISHED` — fired once after the AI backend finishes.
 *   The gateway emits `{ type, result }`; `result` carries the full
 *   persisted `conversation` and a `meta` block.
 * - `RUN_ERROR` — fired when the stream fails. Carries a `message`
 *   and optional `code`; the conversation row is marked FAILED
 *   before close.
 *
 * Forwarded upstream lifecycle or child-run events may contain `runId`,
 * `threadId`, and `parentRunId`. Gateway-generated root terminal events
 * do not.
 * Clients should ignore unknown event names rather than treating them
 * as errors.
 */
export type ConversationStreamSSEEvent = {
  event?: ConversationStreamSSEEventEvent | undefined;
  /**
   * JSON-encoded event payload. The decoded JSON includes a `"type"`
   *
   * @remarks
   * field matching `event`, plus type-specific fields. Shape depends
   * on `event`. Forwarded lifecycle events may carry `runId`,
   * `threadId`, and `parentRunId`.
   */
  data?: string | undefined;
};

/** @internal */
export const ConversationStreamSSEEventEvent$inboundSchema: z.ZodMiniType<
  ConversationStreamSSEEventEvent,
  unknown
> = openEnums.inboundSchema(ConversationStreamSSEEventEvent);

/** @internal */
export const ConversationStreamSSEEvent$inboundSchema: z.ZodMiniType<
  ConversationStreamSSEEvent,
  unknown
> = z.object({
  event: types.optional(ConversationStreamSSEEventEvent$inboundSchema),
  data: types.optional(types.string()),
});

export function conversationStreamSSEEventFromJSON(
  jsonString: string,
): SafeParseResult<ConversationStreamSSEEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConversationStreamSSEEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConversationStreamSSEEvent' from JSON`,
  );
}
