/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Basic request metadata returned by the API.
 */
export type RequestMeta = {
  requestId?: string | undefined;
  timestamp: Date;
  duration: number;
};

/** @internal */
export const RequestMeta$inboundSchema: z.ZodMiniType<RequestMeta, unknown> = z
  .object({
    requestId: types.optional(types.string()),
    timestamp: types.date(),
    duration: types.number(),
  });

export function requestMetaFromJSON(
  jsonString: string,
): SafeParseResult<RequestMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RequestMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RequestMeta' from JSON`,
  );
}
