# GetKnowledgeBaseByIdUserRole

User's role in this knowledge base

## Example Usage

```typescript
import { GetKnowledgeBaseByIdUserRole } from "@pipeshub-ai/sdk/models";

let value: GetKnowledgeBaseByIdUserRole = "READER";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"OWNER" | "WRITER" | "READER" | Unrecognized<string>
```