/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import { FilterOption, FilterOption$inboundSchema } from "./filter-option.js";
import {
  KnowledgeHubNode,
  KnowledgeHubNode$inboundSchema,
} from "./knowledge-hub-node.js";

/**
 * Node being browsed when `parentId` is in the path; `null` at root.
 */
export type CurrentNode = {
  id: string;
  name: string;
  /**
   * One of `app`, `recordGroup`, `folder`, `record`.
   */
  nodeType: string;
  /**
   * Connector name or record type when applicable; otherwise `null`.
   */
  subType?: string | null | undefined;
};

/**
 * Parent of `currentNode` when present; `null` when not applicable.
 */
export type ParentNode = {
  id: string;
  name: string;
  /**
   * One of `app`, `recordGroup`, `folder`, `record`.
   */
  nodeType: string;
  subType?: string | null | undefined;
};

export type KnowledgeHubNodesResponsePagination = {
  /**
   * Current page (1-indexed).
   */
  page: number;
  /**
   * Page size.
   */
  limit: number;
  totalItems: number;
  totalPages: number;
  hasNext: boolean;
  hasPrev: boolean;
};

/**
 * Date range filter with optional inclusive bounds (epoch ms).
 */
export type CreatedAt = {
  /**
   * Greater-than-or-equal bound (epoch ms).
   */
  gte?: number | null | undefined;
  /**
   * Less-than-or-equal bound (epoch ms).
   */
  lte?: number | null | undefined;
};

/**
 * Date range filter with optional inclusive bounds (epoch ms).
 */
export type UpdatedAt = {
  /**
   * Greater-than-or-equal bound (epoch ms).
   */
  gte?: number | null | undefined;
  /**
   * Less-than-or-equal bound (epoch ms).
   */
  lte?: number | null | undefined;
};

/**
 * Size range filter with optional inclusive bounds (bytes).
 */
export type Size = {
  /**
   * Greater-than-or-equal bound (bytes).
   */
  gte?: number | null | undefined;
  /**
   * Less-than-or-equal bound (bytes).
   */
  lte?: number | null | undefined;
};

/**
 * Echo of applied filters; unused slots are JSON `null`.
 */
export type Applied = {
  q: string | null;
  nodeTypes: Array<string> | null;
  recordTypes: Array<string> | null;
  origins: Array<string> | null;
  connectorIds: Array<string> | null;
  indexingStatus: Array<string> | null;
  createdAt: CreatedAt | null;
  updatedAt: UpdatedAt | null;
  size: Size | null;
  /**
   * Effective sort field after server normalisation.
   */
  sortBy: string;
  /**
   * Effective sort order after server normalisation.
   */
  sortOrder: string;
};

/**
 * Populated when `include=availableFilters`; otherwise `null`.
 */
export type Available = {
  nodeTypes: Array<FilterOption>;
  recordTypes: Array<FilterOption>;
  origins: Array<FilterOption>;
  connectors: Array<FilterOption>;
  indexingStatus: Array<FilterOption>;
  sortBy: Array<FilterOption>;
  sortOrder: Array<FilterOption>;
};

export type KnowledgeHubNodesResponseFilters = {
  /**
   * Echo of applied filters; unused slots are JSON `null`.
   */
  applied: Applied;
  /**
   * Populated when `include=availableFilters`; otherwise `null`.
   */
  available: Available | null;
};

export type Breadcrumb = {
  id: string;
  name: string;
  /**
   * One of `app`, `recordGroup`, `folder`, `record`.
   */
  nodeType: string;
  subType?: string | null | undefined;
};

export type Item = {
  label: string;
  count: number;
};

/**
 * Present when `include=counts`; otherwise `null`.
 */
export type Counts = {
  items: Array<Item>;
  total: number;
};

/**
 * Present when `include=permissions`; otherwise `null`.
 */
export type Permissions = {
  role: string;
  canUpload: boolean;
  canCreateFolders: boolean;
  canEdit: boolean;
  canDelete: boolean;
  canManagePermissions: boolean;
};

/**
 * Response body for the Knowledge Hub nodes API. The deployed service
 *
 * @remarks
 * serialises optional values as JSON `null` and always includes the keys
 * listed in `required` (Swagger / clients will see stable shapes, not
 * omitted properties).
 */
export type KnowledgeHubNodesResponse = {
  /**
   * Always `true` on HTTP 200. Failures use 4xx/5xx error envelopes, not this body shape.
   */
  success: boolean;
  /**
   * Always `null` on HTTP 200.
   */
  error: string | null;
  /**
   * Current parent node ID when browsing children; `null` at root.
   */
  id: string | null;
  /**
   * Node being browsed when `parentId` is in the path; `null` at root.
   */
  currentNode: CurrentNode | null;
  /**
   * Parent of `currentNode` when present; `null` when not applicable.
   */
  parentNode: ParentNode | null;
  /**
   * Page of nodes for the current browse or search.
   */
  items: Array<KnowledgeHubNode>;
  pagination: KnowledgeHubNodesResponsePagination;
  filters: KnowledgeHubNodesResponseFilters;
  /**
   * Present when `include=breadcrumbs`; otherwise `null`.
   */
  breadcrumbs: Array<Breadcrumb> | null;
  /**
   * Present when `include=counts`; otherwise `null`.
   */
  counts: Counts | null;
  /**
   * Present when `include=permissions`; otherwise `null`.
   */
  permissions: Permissions | null;
};

/** @internal */
export const CurrentNode$inboundSchema: z.ZodMiniType<CurrentNode, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    nodeType: types.string(),
    subType: z.optional(z.nullable(types.string())),
  });

export function currentNodeFromJSON(
  jsonString: string,
): SafeParseResult<CurrentNode, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CurrentNode$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CurrentNode' from JSON`,
  );
}

/** @internal */
export const ParentNode$inboundSchema: z.ZodMiniType<ParentNode, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    nodeType: types.string(),
    subType: z.optional(z.nullable(types.string())),
  });

export function parentNodeFromJSON(
  jsonString: string,
): SafeParseResult<ParentNode, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ParentNode$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ParentNode' from JSON`,
  );
}

/** @internal */
export const KnowledgeHubNodesResponsePagination$inboundSchema: z.ZodMiniType<
  KnowledgeHubNodesResponsePagination,
  unknown
> = z.object({
  page: types.number(),
  limit: types.number(),
  totalItems: types.number(),
  totalPages: types.number(),
  hasNext: types.boolean(),
  hasPrev: types.boolean(),
});

export function knowledgeHubNodesResponsePaginationFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeHubNodesResponsePagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      KnowledgeHubNodesResponsePagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeHubNodesResponsePagination' from JSON`,
  );
}

/** @internal */
export const CreatedAt$inboundSchema: z.ZodMiniType<CreatedAt, unknown> = z
  .object({
    gte: z.optional(z.nullable(types.number())),
    lte: z.optional(z.nullable(types.number())),
  });

export function createdAtFromJSON(
  jsonString: string,
): SafeParseResult<CreatedAt, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreatedAt$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreatedAt' from JSON`,
  );
}

/** @internal */
export const UpdatedAt$inboundSchema: z.ZodMiniType<UpdatedAt, unknown> = z
  .object({
    gte: z.optional(z.nullable(types.number())),
    lte: z.optional(z.nullable(types.number())),
  });

export function updatedAtFromJSON(
  jsonString: string,
): SafeParseResult<UpdatedAt, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdatedAt$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdatedAt' from JSON`,
  );
}

/** @internal */
export const Size$inboundSchema: z.ZodMiniType<Size, unknown> = z.object({
  gte: z.optional(z.nullable(types.number())),
  lte: z.optional(z.nullable(types.number())),
});

export function sizeFromJSON(
  jsonString: string,
): SafeParseResult<Size, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Size$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Size' from JSON`,
  );
}

/** @internal */
export const Applied$inboundSchema: z.ZodMiniType<Applied, unknown> = z.object({
  q: types.nullable(types.string()),
  nodeTypes: types.nullable(z.array(types.string())),
  recordTypes: types.nullable(z.array(types.string())),
  origins: types.nullable(z.array(types.string())),
  connectorIds: types.nullable(z.array(types.string())),
  indexingStatus: types.nullable(z.array(types.string())),
  createdAt: types.nullable(z.lazy(() => CreatedAt$inboundSchema)),
  updatedAt: types.nullable(z.lazy(() => UpdatedAt$inboundSchema)),
  size: types.nullable(z.lazy(() => Size$inboundSchema)),
  sortBy: types.string(),
  sortOrder: types.string(),
});

export function appliedFromJSON(
  jsonString: string,
): SafeParseResult<Applied, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Applied$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Applied' from JSON`,
  );
}

/** @internal */
export const Available$inboundSchema: z.ZodMiniType<Available, unknown> = z
  .object({
    nodeTypes: z.array(FilterOption$inboundSchema),
    recordTypes: z.array(FilterOption$inboundSchema),
    origins: z.array(FilterOption$inboundSchema),
    connectors: z.array(FilterOption$inboundSchema),
    indexingStatus: z.array(FilterOption$inboundSchema),
    sortBy: z.array(FilterOption$inboundSchema),
    sortOrder: z.array(FilterOption$inboundSchema),
  });

export function availableFromJSON(
  jsonString: string,
): SafeParseResult<Available, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Available$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Available' from JSON`,
  );
}

/** @internal */
export const KnowledgeHubNodesResponseFilters$inboundSchema: z.ZodMiniType<
  KnowledgeHubNodesResponseFilters,
  unknown
> = z.object({
  applied: z.lazy(() => Applied$inboundSchema),
  available: types.nullable(z.lazy(() => Available$inboundSchema)),
});

export function knowledgeHubNodesResponseFiltersFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeHubNodesResponseFilters, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeHubNodesResponseFilters$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeHubNodesResponseFilters' from JSON`,
  );
}

/** @internal */
export const Breadcrumb$inboundSchema: z.ZodMiniType<Breadcrumb, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    nodeType: types.string(),
    subType: z.optional(z.nullable(types.string())),
  });

export function breadcrumbFromJSON(
  jsonString: string,
): SafeParseResult<Breadcrumb, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Breadcrumb$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Breadcrumb' from JSON`,
  );
}

/** @internal */
export const Item$inboundSchema: z.ZodMiniType<Item, unknown> = z.object({
  label: types.string(),
  count: types.number(),
});

export function itemFromJSON(
  jsonString: string,
): SafeParseResult<Item, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Item$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Item' from JSON`,
  );
}

/** @internal */
export const Counts$inboundSchema: z.ZodMiniType<Counts, unknown> = z.object({
  items: z.array(z.lazy(() => Item$inboundSchema)),
  total: types.number(),
});

export function countsFromJSON(
  jsonString: string,
): SafeParseResult<Counts, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Counts$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Counts' from JSON`,
  );
}

/** @internal */
export const Permissions$inboundSchema: z.ZodMiniType<Permissions, unknown> = z
  .object({
    role: types.string(),
    canUpload: types.boolean(),
    canCreateFolders: types.boolean(),
    canEdit: types.boolean(),
    canDelete: types.boolean(),
    canManagePermissions: types.boolean(),
  });

export function permissionsFromJSON(
  jsonString: string,
): SafeParseResult<Permissions, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Permissions$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Permissions' from JSON`,
  );
}

/** @internal */
export const KnowledgeHubNodesResponse$inboundSchema: z.ZodMiniType<
  KnowledgeHubNodesResponse,
  unknown
> = z.object({
  success: types.boolean(),
  error: types.nullable(types.string()),
  id: types.nullable(types.string()),
  currentNode: types.nullable(z.lazy(() => CurrentNode$inboundSchema)),
  parentNode: types.nullable(z.lazy(() => ParentNode$inboundSchema)),
  items: z.array(KnowledgeHubNode$inboundSchema),
  pagination: z.lazy(() => KnowledgeHubNodesResponsePagination$inboundSchema),
  filters: z.lazy(() => KnowledgeHubNodesResponseFilters$inboundSchema),
  breadcrumbs: types.nullable(z.array(z.lazy(() => Breadcrumb$inboundSchema))),
  counts: types.nullable(z.lazy(() => Counts$inboundSchema)),
  permissions: types.nullable(z.lazy(() => Permissions$inboundSchema)),
});

export function knowledgeHubNodesResponseFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeHubNodesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeHubNodesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeHubNodesResponse' from JSON`,
  );
}
