/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A skill linked to the agent, as returned by the agent detail graph
 *
 * @remarks
 * projection. Flat by design — a skill carries no sub-entities analogous
 * to a toolset's tools. Fields other than `name` are read straight off
 * the skill document and are null when unset.
 */
export type AgentSkill = {
  /**
   * Unique skill name, used to reference the skill on agent create/update.
   */
  name: string;
  description?: string | null | undefined;
  category?: string | null | undefined;
  subcategory?: string | null | undefined;
  version?: string | null | undefined;
  /**
   * Lifecycle state of the skill — `active` or `deprecated`.
   */
  status?: string | null | undefined;
};

/** @internal */
export const AgentSkill$inboundSchema: z.ZodMiniType<AgentSkill, unknown> = z
  .object({
    name: types.string(),
    description: z.optional(z.nullable(types.string())),
    category: z.optional(z.nullable(types.string())),
    subcategory: z.optional(z.nullable(types.string())),
    version: z.optional(z.nullable(types.string())),
    status: z.optional(z.nullable(types.string())),
  });

export function agentSkillFromJSON(
  jsonString: string,
): SafeParseResult<AgentSkill, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentSkill$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentSkill' from JSON`,
  );
}
