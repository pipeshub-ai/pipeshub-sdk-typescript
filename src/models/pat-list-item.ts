/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A personal access token as seen by its own creator (one element of
 *
 * @remarks
 * `GET /personal-access-tokens`'s `tokens` array, or the `token` object
 * returned by `POST /personal-access-tokens` before `accessToken` is added).
 */
export type PatListItem = {
  /**
   * Token ID
   */
  id: string;
  /**
   * Token name
   */
  name: string;
  /**
   * Granted scopes
   */
  scopes: Array<string>;
  createdAt: Date;
  /**
   * Expiry timestamp. A `"never"`-expiry token is stored as a
   *
   * @remarks
   * ~100-year-out date, not a literal null — treat anything decades
   * out as "never" rather than a real deadline.
   */
  expiresAt: Date;
  /**
   * Last time this token successfully authenticated a request.
   *
   * @remarks
   * Throttled server-side to update at most once per 5 minutes per
   * token; absent if the token has never been used.
   */
  lastUsedAt?: Date | undefined;
};

/** @internal */
export const PatListItem$inboundSchema: z.ZodMiniType<PatListItem, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    scopes: z.array(types.string()),
    createdAt: types.date(),
    expiresAt: types.date(),
    lastUsedAt: types.optional(types.date()),
  });

export function patListItemFromJSON(
  jsonString: string,
): SafeParseResult<PatListItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatListItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatListItem' from JSON`,
  );
}
