/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  WebSearchProviderType,
  WebSearchProviderType$inboundSchema,
} from "./web-search-provider-type.js";

/**
 * Web search provider configuration item returned by getWebSearchProviders
 */
export type WebSearchProviderItem = {
  /**
   * Supported web search provider
   */
  provider: WebSearchProviderType;
  /**
   * Unique key for the provider configuration
   */
  providerKey: string;
  /**
   * Provider-specific configuration as stored and returned by the gateway (open record).
   *
   * @remarks
   * Serper, Tavily, and Exa typically include `apiKey`; additional keys may be present.
   */
  configuration: { [k: string]: any };
  isDefault: boolean;
};

/** @internal */
export const WebSearchProviderItem$inboundSchema: z.ZodMiniType<
  WebSearchProviderItem,
  unknown
> = z.object({
  provider: WebSearchProviderType$inboundSchema,
  providerKey: types.string(),
  configuration: z.record(z.string(), z.any()),
  isDefault: types.boolean(),
});

export function webSearchProviderItemFromJSON(
  jsonString: string,
): SafeParseResult<WebSearchProviderItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebSearchProviderItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebSearchProviderItem' from JSON`,
  );
}
