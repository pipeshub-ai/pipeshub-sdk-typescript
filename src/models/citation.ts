/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  PersistedSemanticSearchCitationMetadata,
  PersistedSemanticSearchCitationMetadata$inboundSchema,
} from "./persisted-semantic-search-citation-metadata.js";

/**
 * A populated citation document. Represents a single chunk of source
 *
 * @remarks
 * content (e.g. a passage from a document or record) referenced by an
 * AI response, together with its provenance metadata.
 */
export type Citation = {
  id: string;
  /**
   * The cited text chunk
   */
  content: string;
  /**
   * Index of this chunk within the source record
   */
  chunkIndex: number;
  /**
   * Source type identifier (e.g. `vectordb|document`)
   */
  citationType: string;
  /**
   * Citation metadata as persisted in MongoDB. Required fields mirror the
   *
   * @remarks
   * Mongoose schema's `required: true` flags; the rest are optional and
   * may come through as `null` because the AI retrieval service emits
   * explicit nulls for absent fields.
   */
  metadata: PersistedSemanticSearchCitationMetadata;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const Citation$inboundSchema: z.ZodMiniType<Citation, unknown> = z.pipe(
  z.object({
    _id: types.string(),
    content: types.string(),
    chunkIndex: types.number(),
    citationType: types.string(),
    metadata: PersistedSemanticSearchCitationMetadata$inboundSchema,
    createdAt: types.date(),
    updatedAt: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_id": "id",
    });
  }),
);

export function citationFromJSON(
  jsonString: string,
): SafeParseResult<Citation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Citation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Citation' from JSON`,
  );
}
