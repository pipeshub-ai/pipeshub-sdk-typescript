/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import { Knowledge, Knowledge$inboundSchema } from "./knowledge.js";
import { Toolset, Toolset$inboundSchema } from "./toolset.js";

/**
 * Web-search provider attachment for this agent, or `null` when none is attached.
 *
 * @remarks
 *
 * For `GET /agents`, the response formatter always emits `provider`.
 * It may also emit `providerKey` and `providerLabel` when those values
 * were present on the stored attachment. It does not emit `iconPath`
 * on this response path.
 */
export type AgentListItemWebSearch = {
  provider?: string | undefined;
  providerKey?: string | undefined;
  providerLabel?: string | undefined;
};

/**
 * Agent projection returned by `GET /agents`.
 *
 * @remarks
 *
 * This is the list-view envelope item emitted by the Python backend and
 * forwarded by the Node gateway. It is not the full detail projection
 * used by `GET /agents/{agentKey}`.
 */
export type AgentListItem = {
  /**
   * Full document id in the backing graph store.
   */
  id: string;
  /**
   * Stable agent key used in route params.
   */
  key: string;
  /**
   * Backend document revision token.
   */
  rev?: string | undefined;
  /**
   * Unix epoch timestamp in milliseconds when the agent was created.
   */
  createdAtTimestamp: number;
  /**
   * MongoDB user ID of the agent creator
   */
  createdBy: string;
  /**
   * Short human-readable description of the agent.
   */
  description?: string | null | undefined;
  /**
   * Additional execution instructions stored on the agent.
   */
  instructions?: string | null | undefined;
  /**
   * Whether the agent is active.
   */
  isActive: boolean;
  /**
   * Whether the agent has been soft-deleted.
   */
  isDeleted: boolean;
  /**
   * Whether this agent is a service-account agent.
   */
  isServiceAccount: boolean;
  /**
   * Model entries configured on the agent.
   *
   * @remarks
   *
   * For `GET /agents`, the backend returns the stored normalized string
   * representation, typically `modelKey_modelName`.
   */
  models: Array<string>;
  /**
   * Display name of the agent.
   */
  name: string;
  /**
   * Greeting shown at conversation start.
   */
  startMessage?: string | null | undefined;
  /**
   * System prompt stored on the agent.
   */
  systemPrompt?: string | null | undefined;
  /**
   * Free-form agent tags.
   */
  tags: Array<string>;
  /**
   * Unix epoch timestamp in milliseconds when the agent was last updated.
   */
  updatedAtTimestamp: number;
  /**
   * User id of the last updater, if present.
   */
  updatedBy?: string | null | undefined;
  /**
   * Web-search provider attachment for this agent, or `null` when none is attached.
   *
   * @remarks
   *
   * For `GET /agents`, the response formatter always emits `provider`.
   * It may also emit `providerKey` and `providerLabel` when those values
   * were present on the stored attachment. It does not emit `iconPath`
   * on this response path.
   */
  webSearch?: AgentListItemWebSearch | null | undefined;
  /**
   * Whether the agent is shared with the organization.
   */
  shareWithOrg: boolean;
  /**
   * Toolset instances linked to the agent. Same projection as
   *
   * @remarks
   * `GET /agents/{agentKey}`; the backend builds it from the graph edges
   * for each agent on the returned page.
   */
  toolsets: Array<Toolset>;
  /**
   * Knowledge connectors and indexed scopes linked to the agent. Same
   *
   * @remarks
   * projection as `GET /agents/{agentKey}`; the backend builds it from
   * the graph edges for each agent on the returned page.
   */
  knowledge: Array<Knowledge>;
  /**
   * Effective permission to view the agent.
   */
  canView: boolean;
  /**
   * Effective permission to share the agent.
   */
  canShare: boolean;
  /**
   * Effective permission to edit the agent.
   */
  canEdit: boolean;
  /**
   * Effective permission to delete the agent.
   */
  canDelete: boolean;
  /**
   * Effective role of the current user on this agent.
   */
  userRole: string;
  /**
   * How the user can access this agent.
   */
  accessType: string;
};

/** @internal */
export const AgentListItemWebSearch$inboundSchema: z.ZodMiniType<
  AgentListItemWebSearch,
  unknown
> = z.object({
  provider: types.optional(types.string()),
  providerKey: types.optional(types.string()),
  providerLabel: types.optional(types.string()),
});

export function agentListItemWebSearchFromJSON(
  jsonString: string,
): SafeParseResult<AgentListItemWebSearch, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentListItemWebSearch$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentListItemWebSearch' from JSON`,
  );
}

/** @internal */
export const AgentListItem$inboundSchema: z.ZodMiniType<
  AgentListItem,
  unknown
> = z.pipe(
  z.object({
    _id: types.string(),
    _key: types.string(),
    _rev: types.optional(types.string()),
    createdAtTimestamp: types.number(),
    createdBy: types.string(),
    description: z.optional(z.nullable(types.string())),
    instructions: z.optional(z.nullable(types.string())),
    isActive: types.boolean(),
    isDeleted: types.boolean(),
    isServiceAccount: types.boolean(),
    models: z.array(types.string()),
    name: types.string(),
    startMessage: z.optional(z.nullable(types.string())),
    systemPrompt: z.optional(z.nullable(types.string())),
    tags: z.array(types.string()),
    updatedAtTimestamp: types.number(),
    updatedBy: z.optional(z.nullable(types.string())),
    webSearch: z.optional(
      z.nullable(z.lazy(() => AgentListItemWebSearch$inboundSchema)),
    ),
    shareWithOrg: types.boolean(),
    toolsets: z.array(Toolset$inboundSchema),
    knowledge: z.array(Knowledge$inboundSchema),
    can_view: types.boolean(),
    can_share: types.boolean(),
    can_edit: types.boolean(),
    can_delete: types.boolean(),
    user_role: types.string(),
    access_type: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_id": "id",
      "_key": "key",
      "_rev": "rev",
      "can_view": "canView",
      "can_share": "canShare",
      "can_edit": "canEdit",
      "can_delete": "canDelete",
      "user_role": "userRole",
      "access_type": "accessType",
    });
  }),
);

export function agentListItemFromJSON(
  jsonString: string,
): SafeParseResult<AgentListItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentListItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentListItem' from JSON`,
  );
}
