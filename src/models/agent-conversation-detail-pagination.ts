/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type MessageRange = {
  start: number;
  end: number;
};

/**
 * Message pagination returned inside the `conversation` object. The
 *
 * @remarks
 * handler paginates backwards from the end of the stored message array,
 * then sorts the selected page in memory before serialization.
 */
export type AgentConversationDetailPagination = {
  page: number;
  limit: number;
  totalCount: number;
  totalPages: number;
  /**
   * True when older messages exist outside the returned page
   */
  hasNextPage: boolean;
  /**
   * True when newer messages exist outside the returned page
   */
  hasPrevPage: boolean;
  messageRange: MessageRange;
};

/** @internal */
export const MessageRange$inboundSchema: z.ZodMiniType<MessageRange, unknown> =
  z.object({
    start: types.number(),
    end: types.number(),
  });

export function messageRangeFromJSON(
  jsonString: string,
): SafeParseResult<MessageRange, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MessageRange$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MessageRange' from JSON`,
  );
}

/** @internal */
export const AgentConversationDetailPagination$inboundSchema: z.ZodMiniType<
  AgentConversationDetailPagination,
  unknown
> = z.object({
  page: types.number(),
  limit: types.number(),
  totalCount: types.number(),
  totalPages: types.number(),
  hasNextPage: types.boolean(),
  hasPrevPage: types.boolean(),
  messageRange: z.lazy(() => MessageRange$inboundSchema),
});

export function agentConversationDetailPaginationFromJSON(
  jsonString: string,
): SafeParseResult<AgentConversationDetailPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentConversationDetailPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentConversationDetailPagination' from JSON`,
  );
}
