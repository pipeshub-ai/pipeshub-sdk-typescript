/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  AgentConversationListItem,
  AgentConversationListItem$inboundSchema,
} from "./agent-conversation-list-item.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  SemanticSearchHistoryFilters,
  SemanticSearchHistoryFilters$inboundSchema,
} from "./semantic-search-history-filters.js";
import {
  SemanticSearchHistoryMeta,
  SemanticSearchHistoryMeta$inboundSchema,
} from "./semantic-search-history-meta.js";
import {
  SemanticSearchHistoryPagination,
  SemanticSearchHistoryPagination$inboundSchema,
} from "./semantic-search-history-pagination.js";

/**
 * Envelope returned by `GET /agents/{agentKey}/conversations`.
 *
 * @remarks
 * `conversations` contains rows owned by the caller for the agent;
 * `sharedWithMeConversations` contains rows shared with the caller for
 * the same agent. Both arrays use the same pagination and sort inputs,
 * but `pagination.totalCount` and `totalPages` are computed only from
 * `conversations` because the handler counts the owned-query filter only.
 */
export type AgentConversationListResponse = {
  conversations: Array<AgentConversationListItem>;
  sharedWithMeConversations: Array<AgentConversationListItem>;
  /**
   * Pagination block emitted by `buildPaginationMetadata` (utils.ts:417).
   *
   * @remarks
   * `totalPages` is `Math.ceil(totalCount / limit)`, so an empty result
   * has `totalPages: 0`, not `1`.
   */
  pagination: SemanticSearchHistoryPagination;
  filters: SemanticSearchHistoryFilters;
  /**
   * `requestId` comes from `req.context?.requestId` and is omitted from
   *
   * @remarks
   * the JSON when upstream middleware did not set it.
   */
  meta: SemanticSearchHistoryMeta;
};

/** @internal */
export const AgentConversationListResponse$inboundSchema: z.ZodMiniType<
  AgentConversationListResponse,
  unknown
> = z.object({
  conversations: z.array(AgentConversationListItem$inboundSchema),
  sharedWithMeConversations: z.array(AgentConversationListItem$inboundSchema),
  pagination: SemanticSearchHistoryPagination$inboundSchema,
  filters: SemanticSearchHistoryFilters$inboundSchema,
  meta: SemanticSearchHistoryMeta$inboundSchema,
});

export function agentConversationListResponseFromJSON(
  jsonString: string,
): SafeParseResult<AgentConversationListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentConversationListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentConversationListResponse' from JSON`,
  );
}
