/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Normalized web search global settings returned by getWebSearchProviders
 */
export type WebSearchSettings = {
  /**
   * Whether to include images in search results
   */
  includeImages: boolean;
  /**
   * Maximum number of images to return when includeImages is true
   */
  maxImages?: number | undefined;
};

/** @internal */
export const WebSearchSettings$inboundSchema: z.ZodMiniType<
  WebSearchSettings,
  unknown
> = z.object({
  includeImages: types.boolean(),
  maxImages: types.optional(types.number()),
});

export function webSearchSettingsFromJSON(
  jsonString: string,
): SafeParseResult<WebSearchSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebSearchSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebSearchSettings' from JSON`,
  );
}
