/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  PersistedSemanticSearchCitation,
  PersistedSemanticSearchCitation$inboundSchema,
} from "./persisted-semantic-search-citation.js";
import {
  PersistedSemanticSearchSharedWithEntry,
  PersistedSemanticSearchSharedWithEntry$inboundSchema,
} from "./persisted-semantic-search-shared-with-entry.js";

/**
 * Persisted search document as returned to clients.
 *
 * @remarks
 * `records` is a string-valued map: each value is `JSON.stringify(<record>)`,
 * keyed by the source record's `_id` or `_key`. Clients must `JSON.parse`
 * each value to recover the underlying record object (whose shape resembles
 * `SemanticSearchGraphRecord`). This intentionally differs from the
 * `searchResponse.records` array on the POST `/search` response, which is
 * passed through from the retrieval service untouched.
 */
export type PersistedSemanticSearch = {
  id: string;
  v: number;
  query: string;
  limit: number;
  orgId: string;
  userId: string;
  citationIds: Array<PersistedSemanticSearchCitation>;
  /**
   * Map of source-record id (or `_key`) to a JSON-stringified record object.
   *
   * @remarks
   * Clients must `JSON.parse` each value before reading fields.
   */
  records: { [k: string]: string };
  isShared: boolean;
  /**
   * Set once the search has been shared via `/search/{searchId}/share`.
   */
  shareLink?: string | undefined;
  sharedWith: Array<PersistedSemanticSearchSharedWithEntry>;
  isArchived: boolean;
  /**
   * User ID of the last user who archived this row, or `null` after
   *
   * @remarks
   * an unarchive cleared the archive state. Absent on rows that have
   * never been archived. Currently-archived rows cannot reach this
   * endpoint because `buildFilter` enforces `isArchived: false`.
   */
  archivedBy?: string | null | undefined;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const PersistedSemanticSearch$inboundSchema: z.ZodMiniType<
  PersistedSemanticSearch,
  unknown
> = z.pipe(
  z.object({
    _id: types.string(),
    __v: types.number(),
    query: types.string(),
    limit: types.number(),
    orgId: types.string(),
    userId: types.string(),
    citationIds: z.array(PersistedSemanticSearchCitation$inboundSchema),
    records: z.record(z.string(), types.string()),
    isShared: types.boolean(),
    shareLink: types.optional(types.string()),
    sharedWith: z.array(PersistedSemanticSearchSharedWithEntry$inboundSchema),
    isArchived: types.boolean(),
    archivedBy: z.optional(z.nullable(types.string())),
    createdAt: types.date(),
    updatedAt: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_id": "id",
      "__v": "v",
    });
  }),
);

export function persistedSemanticSearchFromJSON(
  jsonString: string,
): SafeParseResult<PersistedSemanticSearch, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PersistedSemanticSearch$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PersistedSemanticSearch' from JSON`,
  );
}
