/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  KnowledgeGraphLookupMatchSchema,
  KnowledgeGraphLookupMatchSchema$inboundSchema,
} from "./knowledge-graph-lookup-match-schema.js";

/**
 * Response returned by GET /connectors/record/lookup.
 *
 * @remarks
 *
 * Field names are snake_case, matching the knowledge graph's internal
 * view models. A miss is a 200 with an empty `matches` and the input
 * echoed in `not_found_identifiers` — see the endpoint description.
 */
export type LookupRecordResponseSchema = {
  matches: Array<KnowledgeGraphLookupMatchSchema>;
  /**
   * True when one identifier resolved to more than one accessible record. Present the choice rather than taking the first, or retry with `connectorName`.
   */
  ambiguous: boolean;
  /**
   * Identifiers that resolved to nothing the caller can access.
   */
  notFoundIdentifiers: Array<string>;
  /**
   * Per-identifier list of the connector types that were searched, so a miss can be retried against a different `connectorName`. Keys are the identifiers exactly as supplied.
   */
  searchedConnectors?: { [k: string]: Array<string> } | undefined;
  /**
   * Flat-text rendering of the matches — each match's metadata block followed by a `Next:` line naming a follow-up call — and a line per miss naming the connectors that were searched. Capped at 25,000 bytes.
   */
  text: string;
};

/** @internal */
export const LookupRecordResponseSchema$inboundSchema: z.ZodMiniType<
  LookupRecordResponseSchema,
  unknown
> = z.pipe(
  z.object({
    matches: z.array(KnowledgeGraphLookupMatchSchema$inboundSchema),
    ambiguous: types.boolean(),
    not_found_identifiers: z.array(types.string()),
    searched_connectors: types.optional(
      z.record(z.string(), z.array(types.string())),
    ),
    text: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "not_found_identifiers": "notFoundIdentifiers",
      "searched_connectors": "searchedConnectors",
    });
  }),
);

export function lookupRecordResponseSchemaFromJSON(
  jsonString: string,
): SafeParseResult<LookupRecordResponseSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LookupRecordResponseSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LookupRecordResponseSchema' from JSON`,
  );
}
