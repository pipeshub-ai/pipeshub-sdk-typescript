/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  SemanticSearchAppliedFilters,
  SemanticSearchAppliedFilters$inboundSchema,
} from "./semantic-search-applied-filters.js";
import {
  SemanticSearchGraphRecord,
  SemanticSearchGraphRecord$inboundSchema,
} from "./semantic-search-graph-record.js";
import {
  SemanticSearchHit,
  SemanticSearchHit$inboundSchema,
} from "./semantic-search-hit.js";

/**
 * Payload returned by the AI retrieval service for a semantic search (embedded in `searchResponse`).
 *
 * @remarks
 * Optional `virtual_to_record_map` maps each virtual record id (string key) to one resolved graph record document.
 * Empty responses from the retrieval layer omit `virtual_to_record_map`; success payloads may include it alongside hits and records.
 */
export type SemanticSearchAiResponse = {
  searchResults: Array<SemanticSearchHit>;
  records: Array<SemanticSearchGraphRecord>;
  status: string;
  statusCode: number;
  message: string;
  /**
   * Present when KB filters were applied to the search request.
   */
  appliedFilters?: SemanticSearchAppliedFilters | undefined;
  /**
   * Maps virtual record id (object property name) to the accessible graph record document for that id.
   */
  virtualToRecordMap?: { [k: string]: SemanticSearchGraphRecord } | undefined;
};

/** @internal */
export const SemanticSearchAiResponse$inboundSchema: z.ZodMiniType<
  SemanticSearchAiResponse,
  unknown
> = z.pipe(
  z.object({
    searchResults: z.array(SemanticSearchHit$inboundSchema),
    records: z.array(SemanticSearchGraphRecord$inboundSchema),
    status: types.string(),
    status_code: types.number(),
    message: types.string(),
    appliedFilters: types.optional(SemanticSearchAppliedFilters$inboundSchema),
    virtual_to_record_map: types.optional(
      z.record(z.string(), SemanticSearchGraphRecord$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "status_code": "statusCode",
      "virtual_to_record_map": "virtualToRecordMap",
    });
  }),
);

export function semanticSearchAiResponseFromJSON(
  jsonString: string,
): SafeParseResult<SemanticSearchAiResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SemanticSearchAiResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SemanticSearchAiResponse' from JSON`,
  );
}
