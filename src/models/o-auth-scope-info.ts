/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Information about an OAuth scope
 */
export type OAuthScopeInfo = {
  /**
   * Scope identifier
   */
  name: string;
  /**
   * Human-readable scope description
   */
  description: string;
  /**
   * Scope category for grouping (matches the key under `scopes` on list responses)
   */
  category: string;
  /**
   * Whether end-user consent is required when this scope is requested
   */
  requiresUserConsent: boolean;
};

/** @internal */
export const OAuthScopeInfo$inboundSchema: z.ZodMiniType<
  OAuthScopeInfo,
  unknown
> = z.object({
  name: types.string(),
  description: types.string(),
  category: types.string(),
  requiresUserConsent: types.boolean(),
});

export function oAuthScopeInfoFromJSON(
  jsonString: string,
): SafeParseResult<OAuthScopeInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OAuthScopeInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OAuthScopeInfo' from JSON`,
  );
}
