/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  OAuthScopeInfo,
  OAuthScopeInfo$inboundSchema,
} from "./o-auth-scope-info.js";

/**
 * OAuth scopes available to the signed-in user for app registration, grouped by category label.
 *
 * @remarks
 * Category keys are UI labels (e.g. `Identity`, `Knowledge Base`); each maps to a list of scopes in that group.
 * Categories defined in server config may appear with an **empty array** when every scope in that category is restricted for the caller's role (e.g. non–org-admin users never receive admin-only scopes).
 */
export type OAuthScopesGroupedResponse = {
  /**
   * Map of category display name to scopes in that category
   */
  scopes: { [k: string]: Array<OAuthScopeInfo> };
};

/** @internal */
export const OAuthScopesGroupedResponse$inboundSchema: z.ZodMiniType<
  OAuthScopesGroupedResponse,
  unknown
> = z.object({
  scopes: z.record(z.string(), z.array(OAuthScopeInfo$inboundSchema)),
});

export function oAuthScopesGroupedResponseFromJSON(
  jsonString: string,
): SafeParseResult<OAuthScopesGroupedResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OAuthScopesGroupedResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OAuthScopesGroupedResponse' from JSON`,
  );
}
